import 'package:flutter/material.dart';

class HistoryScreenOffline extends StatelessWidget {
  const HistoryScreenOffline({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'No Internet Connection',
      ),
    );
  }
}
