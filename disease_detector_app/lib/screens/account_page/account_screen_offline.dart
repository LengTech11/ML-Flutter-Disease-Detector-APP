import 'package:flutter/material.dart';

class AccountScreenOffline extends StatelessWidget {
  const AccountScreenOffline({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('No Internet Connection'),
    );
  }
}
